services:
  - type: web
    name: discrete-math-set-ops
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn 'app:create_app()'
    autoDeploy: true
    envVars:
      - key: FLASK_DEBUG
        value: '0'